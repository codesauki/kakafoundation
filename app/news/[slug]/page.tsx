export const dynamic = 'force-dynamic';

import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import Link from 'next/link'; import { Calendar, ArrowLeft, Tag } from 'lucide-react'; import Navbar from '@/components/layout/Navbar'; import Footer from '@/components/layout/Footer'; import CTASection from '@/components/sections/CTASection'; import { prisma } from '@/lib/db'; import { formatDate } from '@/lib/utils';  interface Props { params: { slug: string } }  export async function generateMetadata({ params }: Props): Promise<Metadata> {   try {     const article = await prisma.news.findUnique({ where: { slug: params.slug } });     if (!article) return { title: 'Article Not Found' };     return { title: article.title, description: article.excerpt };   } catch { return { title: 'News' }; } }  export default async function NewsArticlePage({ params }: Props) {   let article;   try {     article = await prisma.news.findUnique({ where: { slug: params.slug, isPublished: true } });   } catch { article = null; }    if (!article) notFound();    return (     <>       <Navbar />       <main>         {/* Hero */}         <section className="relative pt-28 pb-0 bg-navy-gradient overflow-hidden">           <div className="absolute inset-0 bg-noise opacity-30" />           <div className="container-xl relative z-10 pb-16">             <Link href="/news" className="inline-flex items-center gap-2 text-teal-300 hover:text-white text-sm font-medium mb-8 transition-colors">               <ArrowLeft className="w-4 h-4" /> Back to News             </Link>             <div className="flex items-center gap-3 mb-5">               <span className="badge bg-white/10 text-gold-300 border border-white/20">{article.category}</span>               {article.publishedAt && (                 <span className="flex items-center gap-1.5 text-sm text-white/60">                   <Calendar className="w-4 h-4" /> {formatDate(article.publishedAt)}                 </span>               )}             </div>             <h1 className="font-display text-4xl sm:text-5xl lg:text-6xl text-white leading-tight max-w-4xl">               {article.title}             </h1>           </div>            {article.coverImage && (             <div className="container-xl px-0 sm:px-4 lg:px-8 relative z-10">               <div className="h-64 sm:h-80 lg:h-96 overflow-hidden rounded-t-3xl">                 <img src={article.coverImage} alt={article.title} className="w-full h-full object-cover" />               </div>             </div>           )}           {!article.coverImage && <div className="h-12" />}           <div className="absolute bottom-0 left-0 right-0 h-20 bg-gradient-to-t from-white to-transparent" />         </section>          {/* Content */}         <section className="py-16 bg-white">           <div className="container-lg">             <div className="grid lg:grid-cols-3 gap-12">               {/* Article body */}               <div className="lg:col-span-2">                 <p className="text-xl text-navy-600 leading-relaxed font-medium mb-8 border-l-4 border-gold-400 pl-5 italic">                   {article.excerpt}                 </p>                 <div className="prose-foundation">                   {article.content.split('\n\n').map((para: string, i: number) => (                     <p key={i} className="text-navy-600 leading-relaxed mb-5 text-base">{para}</p>                   ))}                 </div>                 {article.tags.length > 0 && (                   <div className="mt-8 pt-6 border-t border-navy-100 flex items-center gap-2 flex-wrap">                     <Tag className="w-4 h-4 text-navy-400" />                     {article.tags.map((tag: string) => (                       <span key={tag} className="px-3 py-1 bg-navy-50 text-navy-600 text-xs font-medium rounded-full">#{tag}</span>                     ))}                   </div>                 )}               </div>                {/* Sidebar */}               <div className="space-y-5">                 <div className="card p-5">                   <div className="text-xs font-bold text-navy-400 uppercase tracking-widest mb-3">Published By</div>                   <div className="flex items-center gap-3">                     <div className="w-10 h-10 rounded-full bg-navy-gradient flex items-center justify-center">                       <span className="text-white font-bold text-sm">K</span>                     </div>                     <div>                       <div className="font-semibold text-navy-800 text-sm">{article.authorName}</div>                       <div className="text-navy-400 text-xs">Official Communication</div>                     </div>                   </div>                 </div>                  <div className="card bg-teal-gradient p-5 text-white">                   <h3 className="font-display text-base font-semibold mb-2">Apply for Scholarship</h3>                   <p className="text-white/80 text-sm mb-4">2026 applications are open. Apply free today.</p>                   <Link href="/apply" className="btn-gold w-full justify-center text-sm">Apply Now</Link>                 </div>                  <div className="card p-5">                   <div className="text-xs font-bold text-navy-400 uppercase tracking-widest mb-3">Share</div>                   <div className="flex gap-2">                     {['Twitter', 'Facebook', 'WhatsApp'].map((s) => (                       <button key={s} className="flex-1 py-2 bg-navy-50 hover:bg-navy-100 text-navy-600 text-xs font-medium rounded-lg transition-colors">                         {s}                       </button>                     ))}                   </div>                 </div>               </div>             </div>           </div>         </section>          <CTASection />       </main>       <Footer />     </>   ); }
